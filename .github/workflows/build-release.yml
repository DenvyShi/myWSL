name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check dotnet
        run: |
          dotnet --version
          dotnet --list-sdks

      - name: Restore
        run: dotnet restore

      - name: Build
        run: |
          dotnet build -c Release 2>&1
          dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish 2>&1

      - name: Upload artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: WslBackupManager
          path: publish/*.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: WslBackupManager

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: WslBackupManager.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
